[Unit]
Description=vault daemon
After=network.target

[Service]
User=root
Group=root
ExecStart=/opt/vault server -config=/etc/vault.d/config.json
ExecReload=/bin/kill -HUP $MAINPID
KillSignal=SIGINT
Restart=on-failure

[Install]
WantedBy=multi-user.target
